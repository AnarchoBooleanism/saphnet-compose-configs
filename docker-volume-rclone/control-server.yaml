services:
  docker-volume-rclone:
    extends:
      file: ./base.yaml
      service: docker-volume-rclone
    environment:
      TARGET_SUBDIR_NAME: control-server
      VOLUME_NAMES: >-
        komodo_mongo-config
        komodo_mongo-data

include:
  - ./base.yaml

services:
  docker-volume-rclone:
    <<: *docker-volume-rclone
    environment:
      <<: *environment
      TARGET_SUBDIR_NAME: control-server
      VOLUME_NAMES: >-
        komodo_mongo-config
        komodo_mongo-data

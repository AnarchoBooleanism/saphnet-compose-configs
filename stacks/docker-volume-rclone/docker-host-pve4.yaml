# To be run, and merged, with the base file
# Example command:
# docker compose -f base.yaml -f docker-host-pve4.yaml up

services:
  docker-volume-rclone:
    environment:
      TARGET_SUBDIR_NAME: docker-host-pve4
      VOLUME_NAMES: >-
        pterodactyl-wing-pve4_wings-config
        pterodactyl-wing-pve4_wings-logs

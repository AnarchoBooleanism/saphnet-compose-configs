## Homepage
A way to see the status of various services in one webpage.

Before deploying, make sure `web_bridge` is set up as a network in Komodo.

When deploying in Komodo, make sure to set these environmental variables with your secrets that are listed within the `saphnet-homepage` repo.
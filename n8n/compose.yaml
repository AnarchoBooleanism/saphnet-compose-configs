# Environment variables to set:
# - POSTGRES_PASSWORD
# - POSTGRES_NON_ROOT_PASSWORD
# - ENCRYPTION_KEY, made with "openssl rand -hex 32"
# - TAILSCALE_IP, if you want to restrict access to n8n port to just Tailscale only (for external Traefik)

x-common:
  TIMEZONE: &timezone "America/Los_Angeles"
  POSTGRES: &postgres
    POSTGRES_USER: &postgres_user "postgres"
    POSTGRES_NON_ROOT_USER: &postgres_non_root_user "postgres_nonroot"
    POSTGRES_DB: &postgres_db "n8n"

x-services: # Base instances of services to customize
  N8N_COMMON: &n8n_common
    image: n8nio/n8n:stable@sha256:53004142b0e93de1e9e31943e601ed9bd4b24ca82a307864ccadf1820b627e5e
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      # Database
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: n8n-postgres
      DB_POSTGRESDB_PORT: "5432"
      DB_POSTGRESDB_DATABASE: *postgres_db
      DB_POSTGRESDB_USER: *postgres_non_root_user
      DB_POSTGRESDB_PASSWORD: "${POSTGRES_NON_ROOT_PASSWORD}"
      EXECUTIONS_MODE: queue
      QUEUE_BULL_REDIS_HOST: n8n-redis
      QUEUE_HEALTH_CHECK_ACTIVE: "true"
      N8N_ENCRYPTION_KEY: "${ENCRYPTION_KEY}"
      # Other
      N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS: "true"
      N8N_HOST: n8n.int.saphnet.xyz
      N8N_PORT: "5678"
      N8N_PROTOCOL: https
      N8N_RUNNERS_ENABLED: "true"
      NODE_ENV: production
      WEBHOOK_URL: https://n8n.int.saphnet.xyz/
      GENERIC_TIMEZONE: *timezone
      TZ: *timezone
    volumes:
      - n8n-storage:/home/node/.n8n
    networks: # When having more networks when merging this, be sure to include n8n_internal in the list too!
      - n8n_internal
    restart: unless-stopped

services:
  postgres:
    image: postgres:16@sha256:e4842c8a99ca99339e1693e6fe5fe62c7becb31991f066f989047dfb2fbf47af
    container_name: n8n-postgres
    ports:
      - "5432:5432"
    environment:
      <<: *postgres
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_NON_ROOT_PASSWORD: "${POSTGRES_NON_ROOT_PASSWORD}"
    volumes:
      - db-storage:/var/lib/postgresql/data
      - ./init-data.sh:/docker-entrypoint-initdb.d/init-data.sh
    networks:
      - n8n_internal
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -h localhost -U postgres -d n8n'] # Be sure to update this to reflect POSTGRES_USER and POSTGRES_DB!
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:6-alpine@sha256:46884be93652d02a96a176ccf173d1040bef365c5706aa7b6a1931caec8bfeef
    container_name: n8n-redis
    volumes:
      - redis-storage:/data
    networks:
      - n8n_internal
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped
  
  n8n:
    <<: *n8n_common
    container_name: n8n
    ports:
      - "${TAILSCALE_IP:-0.0.0.0}:5678:5678"
    labels:
      traefik.enable: "true"
      traefik.http.routers.n8n.rule: Host(`n8n.int.saphnet.xyz`)
      traefik.http.routers.n8n.tls: "true"
      traefik.http.routers.n8n.entrypoints: websecure
      traefik.http.routers.netboot.tls.certresolver: letsencrypt
      traefik.http.routers.netboot.tls.domains[0].main: int.saphnet.xyz
      traefik.http.routers.netboot.tls.domains[0].sans: "*.int.saphnet.xyz"
      traefik.http.middlewares.n8n.headers.SSLRedirect: "true"
      traefik.http.middlewares.n8n.headers.STSSeconds: "315360000"
      traefik.http.middlewares.n8n.headers.browserXSSFilter: "true"
      traefik.http.middlewares.n8n.headers.contentTypeNosniff: "true"
      traefik.http.middlewares.n8n.headers.forceSTSHeader: "true"
      traefik.http.middlewares.n8n.headers.SSLHost: int.saphnet.xyz
      traefik.http.middlewares.n8n.headers.STSIncludeSubdomains: "true"
      traefik.http.middlewares.n8n.headers.STSPreload: "true"
      traefik.http.routers.n8n.middlewares: n8n@docker
    networks:
      - n8n_internal
      - web_bridge
  
  n8n-worker:
    <<: *n8n_common
    container_name: n8n-worker
    command: worker
    depends_on:
      - n8n

volumes:
  db-storage:
  n8n-storage:
  redis-storage:

networks:
  n8n_internal:
  web_bridge:
    name: web_bridge
    external: true
